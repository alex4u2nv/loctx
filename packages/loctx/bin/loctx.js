#!/usr/bin/env node
// Thin alias: `npm install -g loctx` gets the real CLI from @loctx/cli.
// Same process, argv untouched — commander in cli.js sees the args as-is.
import "@loctx/cli/dist/cli.js";
