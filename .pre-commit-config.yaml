repos:
  - repo: local
    hooks:
      - id: claude-precommit-review
        name: Claude staged-diff review
        entry: scripts/claude-precommit-review.sh
        language: script
        pass_filenames: false
        stages: [pre-commit]
        always_run: true

