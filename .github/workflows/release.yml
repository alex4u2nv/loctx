name: release

# Tag push (vX.Y.Z) triggers verify + publish. See CONTRIBUTING.md.
on:
  push:
    tags:
      - "v*"

permissions:
  contents: read
  id-token: write   # npm provenance attestation

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: "https://registry.npmjs.org"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        # cli + mcp resolve core through dist/, so build before verify.
        run: pnpm run build

      - name: Verify (lint + typecheck + test)
        run: pnpm run verify

      - name: Verify version matches tag
        run: |
          tag="${GITHUB_REF#refs/tags/v}"
          for pkg in packages/core apps/cli apps/mcp; do
            v=$(node -p "require('./$pkg/package.json').version")
            if [ "$v" != "$tag" ]; then
              echo "::error::$pkg version $v does not match tag v$tag"
              exit 1
            fi
          done
          echo "All package versions match tag v$tag"

      - name: Publish @loctx/core
        # `pnpm publish` resolves `workspace:^` deps to the current
        # workspace versions at publish time, so consumers get plain
        # semver ranges. --no-git-checks because the tag push leaves
        # an unclean dist/ tree.
        run: pnpm --filter @loctx/core publish --access public --provenance --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @loctx/cli
        run: pnpm --filter @loctx/cli publish --access public --provenance --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @loctx/mcp
        run: pnpm --filter @loctx/mcp publish --access public --provenance --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: |
          tag="${GITHUB_REF#refs/tags/v}"
          {
            echo "## Released v$tag"
            echo ""
            echo "Published to npm:"
            echo "- [@loctx/core](https://www.npmjs.com/package/@loctx/core)"
            echo "- [@loctx/cli](https://www.npmjs.com/package/@loctx/cli)"
            echo "- [@loctx/mcp](https://www.npmjs.com/package/@loctx/mcp)"
          } >> "$GITHUB_STEP_SUMMARY"
